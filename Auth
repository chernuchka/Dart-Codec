import 'dart:async';
import 'dart:convert';ade
              builder: (context, snapshot) 
',
tEmpty) {
      _channel.sink.add(_controller.text);
    }
  }

  @override
  void dispose() {
    _channel.sink.close();
    _controller.dispose();
    super.dispose();
  }
}
